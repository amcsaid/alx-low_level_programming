#include "main.h"

/**
 * _isupper - check the code
 * @c: integer input
 * Description: check if a caracter or not
 * Return: 0 or 1
 */

int _isdigit(int c)
{
	if ((c >= '0') && (c <= '9'))
	{
		return (1);
	}
	else
		return (0);
}
