#include <stddef.h>
#include <stdlib.h>
#include <string.h>

/**
 * _realloc - Allocates memory for an array of ints
 *
 * @ptr: starting int
 * @old_size: ending int
 * @new_size
 *
 * Return: A pointer to the allocated memory, or NULL if nmemb or size is 0
 *         or if malloc fails.
 */
void *_realloc(void *ptr, unsigned int old_size, unsigned int new_size)
{
	return (ptr);
}
